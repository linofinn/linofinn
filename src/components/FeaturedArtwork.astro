---
interface Props {
    slug: string;
}

const { slug } = Astro.props;
---

<section class="featured-artwork">

    <img
        src={`/prints/${slug}/${slug}.jpg`}
        alt={slug}
    />

</section>